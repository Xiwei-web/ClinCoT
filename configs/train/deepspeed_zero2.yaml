deepspeed:
  train_micro_batch_size_per_gpu: 1
  gradient_accumulation_steps: 1
  bf16:
    enabled: true
  zero_optimization:
    stage: 2
    contiguous_gradients: true
    overlap_comm: true
    reduce_scatter: true
